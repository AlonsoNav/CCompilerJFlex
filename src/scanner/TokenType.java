package scanner;

public enum TokenType {
    KEYWORD, 
    ID, 
    OPERATOR,
    NUMERIC_LITERAL,
    STRING_LITERAL,
    INTEGER_LITERAL,
    CHAR_LITERAL,
    BINARY_LITERAL, 
    OCTAL_LITERAL,  
    HEX_LITERAL,
    DOUBLE_LITERAL;
}
